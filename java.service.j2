[Unit]
Description=Manage Java service

[Service]
WorkingDirectory=/opt/prod
ExecStart=/opt/{{ app_name }}/bin/java {{java_param}} -jar {{ app_name }}/{{ app_name }}-v{{ app_version }}.jar
User=apps_user
Type=simple
Restart=on-failure
RestartSec=10

[Install]
WantedBy=multi-user.target